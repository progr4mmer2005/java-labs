package compute;

/**
 * Интерфейс задачи, которую выполняет вычислитель.
 * Реализуется на стороне клиента, сериализуется и передаётся серверу.
 */
public interface Task<T> {
    T execute();
}
